# Decision records

This directory records product and workflow decisions that affect the video-production Skill. Each record should state the problem, selected direction, rejected alternatives, compatibility boundary, and validation evidence.

Decision records do not change runtime behavior by themselves. Runtime authority remains in `SKILL.md`, `references/`, `scripts/`, and the current project contract.
